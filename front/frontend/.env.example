# URL base da API (backend). Copie para .env e ajuste se necessário.
# Desenvolvimento local: http://127.0.0.1:5000
# Acesso de outro dispositivo na rede: http://IP_DO_PC:5000 (use a porta definida em FLASK_PORT no backend)
VITE_API_URL=http://127.0.0.1:5000
