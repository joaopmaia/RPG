# Variáveis de ambiente para scripts executados na raiz do projeto
# (migrations, service/storytelling, etc.). Copie para .env e preencha se necessário.
# O backend Flask usa front/backend/.env (veja front/backend/.env.example).

# MongoDB. Padrão para desenvolvimento local.
MONGO_URI=mongodb://localhost:27017
