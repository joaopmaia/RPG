services:
  mongodb:
    image: mongo:7
    container_name: rpg-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: rpg
    volumes:
      - ./data:/data/db
    restart: unless-stopped
